<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <!--
      Zero external dependencies — Domain is the innermost layer.
      If a package reference appears here, something is wrong with the design.
    -->
  </PropertyGroup>

</Project>
